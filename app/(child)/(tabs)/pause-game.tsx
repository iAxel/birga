import type { ReactElement } from 'react'
import { PlaceholderScreen } from '@/ui/placeholder-screen'

/** Turn-taking game: a familiar sequence in the parent's voice that stops before the next item (SPEC §3). */
export default function PauseGameScreen(): ReactElement {
  return <PlaceholderScreen />
}
