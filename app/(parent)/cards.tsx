import type { ReactElement } from 'react'
import { PlaceholderScreen } from '@/ui/placeholder-screen'

/** Boards and cards: photo, written word and the parent's voice (SPEC §2, §5). */
export default function CardsScreen(): ReactElement {
  return <PlaceholderScreen />
}
