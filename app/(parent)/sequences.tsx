import type { ReactElement } from 'react'
import { PlaceholderScreen } from '@/ui/placeholder-screen'

/** Sequences for the pause game: ordered items of text, voice and an optional image (SPEC §3, §5). */
export default function SequencesScreen(): ReactElement {
  return <PlaceholderScreen />
}
